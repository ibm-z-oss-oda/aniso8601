pipelines:
  default:
    - parallel:
        - step:
            image: python:latest
            script:
              - pip install .[dev,relative]
              - python -m unittest discover aniso8601/tests/
            image: python:3.7
            script:
              - pip install .[dev,relative]
              - python -m unittest discover aniso8601/tests/
            image: python:2.7
            script:
              - pip install .[dev,relative]
              - python -m unittest discover aniso8601/tests/
